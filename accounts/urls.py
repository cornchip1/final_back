from django.urls import path
from . import views

urlpatterns = [
    # path('user/<int:user_pk>/',views.create_profile),
    # path('<int:user_pk>/profile/<int:profile_pk>/',views.profile_detail),
] 
